package com.dzmitryj.lorelens.ui

import com.intellij.openapi.util.IconLoader
import javax.swing.Icon

object LoreIcons {
    /** The Lore mark, sized for tool window stripes and headers. */
    @JvmField
    val ToolWindow: Icon = IconLoader.getIcon("/icons/lorelens.svg", LoreIcons::class.java)
}
